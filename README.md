# Вегетарий АСХТ - Сайт заказа растений

Современный веб-сайт для заказа растений, выращиваемых в вегетарии техникума АСХТ.

## Возможности

- 📱 Адаптивный дизайн для всех устройств
- 📹 Интеграция трех видеокамер для онлайн-трансляции
- 📝 Форма заказа растений с валидацией
- ℹ️ Информация о вегетарии и команде
- 🎨 Современный и красивый интерфейс

## Структура проекта

```
├── index.html      # Главная страница
├── styles.css      # Стили и дизайн
├── script.js       # JavaScript функциональность
└── README.md       # Документация
```

## Установка и запуск

1. Скачайте все файлы проекта
2. Откройте `index.html` в браузере
3. Или используйте локальный сервер:
   ```bash
   # Python
   python -m http.server 8000
   
   # Node.js
   npx http-server
   ```

## Настройка видеокамер

Для подключения реальных видеокамер отредактируйте файл `script.js`:

```javascript
const cameraUrls = {
    camera1: 'https://your-camera-url-1.com/stream',  // URL камеры 1
    camera2: 'https://your-camera-url-2.com/stream',  // URL камеры 2
    camera3: 'https://your-camera-url-3.com/stream'   // URL камеры 3
};
```

### Поддерживаемые форматы:

- **HLS потоки** (.m3u8) - для этого потребуется библиотека HLS.js
- **Обычные видео** (MP4, WebM)
- **YouTube видео** (автоматическое преобразование в embed)
- **RTSP потоки** - требуют специального сервера-шлюза

### Для HLS потоков:

Добавьте в `<head>` файла `index.html`:

```html
<script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
```

## Обработка заказов

В текущей версии заказы сохраняются в `localStorage` браузера. Для полноценной работы необходимо:

1. Создать backend API для обработки заказов
2. Изменить код в `script.js` (функция обработки формы) для отправки данных на сервер

Пример:

```javascript
const response = await fetch('/api/orders', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(formData)
});
```

## Кастомизация

### Изменение цветов

Отредактируйте переменные в `styles.css`:

```css
:root {
    --primary-color: #2d8659;    /* Основной цвет */
    --primary-dark: #1f5d3f;     /* Темный оттенок */
    --accent-color: #ff6b35;      /* Акцентный цвет */
}
```

### Изменение текста

Все тексты находятся в `index.html` и могут быть легко изменены.

## Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## Лицензия

Проект создан для вегетария АСХТ.

## Контакты

Для вопросов и предложений обращайтесь в техникум АСХТ.

